client_max_body_size 20M;

upstream backend {
    server backend:3000;
}

server {
    listen 80;
    server_name _;

    index index.html;
    root /var/www/public/;

    include sites.d/frontend.conf;
}
